@model Product
@{
    ViewData["Title"] = "Home Page";
}


<div class="text-center">
    <h1 class="display-4"></h1>
    @if (Model == null)
    {
        <div class="card-body">
            <p>Извините, товар не найден</p>
            <a class="btn btn-info" asp-controller="Home" asp-action="Index" role="button">Вернуться на главную страницу</a>
        </div>
    }
    else
    {
        <div class="col-3 card shadow">
            <img class="card-img-top" href="@Model.Image" src="@Model.Image" alt="@Model.Name" />
            <div class="card-body">
                <a href="Product/Index/@Model.Id">@Model.Name</a>
                <p>@Model.Cost руб</p>
                <a class="btn btn-success" href="#" role="button">Добавить в корзину</a>
            </div>
        </div>
    }

</div>
