@model Product

<div class="row">
    <div class="col-2">
        @await Html.PartialAsync("AdminLeftMenu")
    </div>
    <div class="col-10">

        <div class="row">
            <div class="col">
                <div class="list-group d-flex align-items-center py-5">
                    <h4>Описание товара</h4>
                    @if (Model == null)
                    {
                        <div class="card-body">
                            <p>Извините, товар не найден</p>
                            <a class="btn btn-info" href="/Home/Index" role="button">Вернуться на главную страницу</a>
                        </div>
                    }
                    else
                    {
                        <div class="row">
                            <div class="col-6">
                                <img class="card-img-top" href="@Model.Image" src="@Model.Image" alt="@Model.Name" />
                            </div>
                            <div class="col-6">


                                <div class="row">
                                    <div class="text-left">
                                        <p>@Model.Name</p>
                                        <p>@Model.Cost руб</p>
                                        <p>@Model.Description</p>
                                    </div>
                                </div>
                                <div class="text-left">
                                    <div>
                                        <a class="btn btn-success" href="/Admin/EditForm?id=@Model.Id" role="button">Изменить</a>
                                    </div>
                                    <br />
                                    <div>
                                        <a class="btn btn-danger" href="/Admin/DeleteProduct?id=@Model.Id" role="button">Удалить</a>
                                    </div>
                                    <br />
                                </div>
                            </div>
                        </div>
                        <a class="btn btn-info" href="/Admin/Home" role="button">Вернуться в панель администратора</a>
                    }

                </div>
            </div>
        </div>
    </div>
</div>
